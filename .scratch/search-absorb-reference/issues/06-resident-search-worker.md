# 06: 常驻搜索线程 + 协作取消

**What to build:** 单一常驻搜索工作线程；UI 覆盖「最新搜索请求」并唤醒 worker；旧任务在阶段边界（召回后/验证批次后/排序前）发现过期则提前结束；完成时仍做代际校验。取消产生的部分结果不得写入缓存。不引入线程池，不排队历史按键。

**Blocked by:** None（在 03 的 spawn+代际结果上加固）

**Status:** ready-for-agent

- [ ] 全局仅一个搜索 worker 线程
- [ ] 待处理请求只保留最新一份（覆盖，不排队）
- [ ] 旧任务在阶段检查点协作退出，不跑到完
- [ ] UI 仍保留代际/查询文本双检；取消产物不进缓存
- [ ] 服务缝测试：慢任务注入时最新优先、旧任务提前结束
- [ ] 连续输入不出现结果回退；`cargo test` 全绿

## Notes

- 前序 03 已完成：后台提交 + 过期丢弃 + 无个性化缓存 + 个性化进截断前。
- 本票只补「停止旧计算」，不改检索算法与排序。
